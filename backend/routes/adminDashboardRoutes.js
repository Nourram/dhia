// import express from 'express';
// import { getAdminDashboardKPIs } from '../controllers/AdminDashboardController.js';
// import { authMiddleware as protect, authorizeRole as authorize } from '../middleware/authMiddleware.js'; 

// const router = express.Router();

// router.use(protect);
// router.use(authorize('admin'));

// router.get('/kpis', getAdminDashboardKPIs);

// export default router;
